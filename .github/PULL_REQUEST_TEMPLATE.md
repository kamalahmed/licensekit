## What this changes

(One paragraph describing the change and why.)

## Linked issue

Closes #

## Type of change

- [ ] Bug fix (non-breaking)
- [ ] New feature (non-breaking)
- [ ] Breaking change (existing customer plugins / DB schema affected)
- [ ] Documentation only
- [ ] Test coverage only

## Verification

- [ ] `composer test` passes locally
- [ ] `composer lint` reports zero errors and zero warnings
- [ ] Plugin Check (PCP) ruleset still clean if I touched non-test source
- [ ] Manually verified the change in a local WordPress install

## Schema / API changes

If this touches `dlm_*` tables, REST endpoint shapes, the SDK config, or webhook payloads — describe the migration path for existing installs.

## Screenshots

(Only if the change is UI-facing.)
